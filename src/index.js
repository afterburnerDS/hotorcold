import React from 'react';
import ReactDOM from 'react-dom';
import Game from './components/game'; // Imported the Board component
 import './index.css';

 ReactDOM.render(
   <Game />, // Rendered the Board component
   document.getElementById('root')
 );
